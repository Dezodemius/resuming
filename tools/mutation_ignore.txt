# Мутанты, разобранные вручную и признанные эквивалентными.
#
# Формат: <хеш>  # причина
# Хеш печатает сам tools/mutation_diff.py рядом с каждым выжившим мутантом —
# строку отсюда можно скопировать прямо из его вывода.
#
# Ключ — хеш изменения, а не имя мутанта: имена нумеруются по порядку и
# разъезжаются при любой правке функции. Если код изменится, хеш перестанет
# совпадать и мутант вернётся под гейт — это намеренно, запись протухает в
# безопасную сторону.
#
# Сюда попадает только то, что действительно эквивалентно: мутант меняет код,
# но не может изменить наблюдаемое поведение. «Лень писать тест» — не причина;
# в таком случае тест и надо написать. Автоматически отсеиваемое (изменения
# внутри log.*(), текст сообщений исключений, регистр литералов) вносить не
# надо, это делает tools/mutation_ignore.py.

# ── main._fetch_job_text ────────────────────────────────────────────────────

# chunks, size = [], 0  ->  1
# Начальное значение счётчика прочитанного. Предел MAX_JOB_BYTES мягкий: чтение
# всё равно обрывается по границе куска, поэтому расхождение в один байт
# наблюдаемого поведения не меняет.
3d587c6b  # начальное значение счётчика байт: предел мягкий, один байт ничего не решает

# status = r.status_code  ->  None
# Переменная используется ровно в одной строке — в log.info с результатом.
# Наружу не выходит.
e8fd1cba  # status нужен только для строки лога

# html, status = "", None  ->  "XXXX"
# Начальное значение html. До использования оно всегда перезаписывается: либо
# тело прочитано в цикле, либо сработал else и функция бросила исключение.
1aa498d6  # недостижимое начальное значение: html всегда перезаписывается до чтения

# if parsed.scheme not in (...) or not parsed.hostname  ->  and
# С `and` ссылка без хоста проходит проверку схемы, но следом
# _assert_public_host(None) резолвит пустой хост в loopback и всё равно бросает
# 400. Итог для вызывающего тот же.
980cd1f2  # ссылку без хоста ниже всё равно отсекает _assert_public_host

# .split(";")  ->  .split("XX;XX")
# Отрезание параметров content-type. Решение принимается через startswith по
# префиксу типа, а параметры идут после него — с ними и без них список
# разрешённых типов даёт один и тот же ответ.
979bcbb9  # параметры content-type не влияют на префиксную проверку типа

# ── main._set_anon_cookie ───────────────────────────────────────────────────

# samesite="lax" убран из вызова
# Starlette по умолчанию ставит samesite="lax", то есть заголовок Set-Cookie
# получается побайтово тем же. Проверять нечем.
0b3ee96b  # без samesite Starlette подставляет тот же lax по умолчанию

# ── main._anon_ip_key ───────────────────────────────────────────────────────

# digest[:32]  ->  digest[:33]
# Длина среза hex-дайджеста произвольна: ключ счётчика остаётся уникальным при
# любой длине от десятка символов.
7f7a70f8  # длина среза hex-дайджеста не влияет на уникальность ключа

# ── main._client_key ────────────────────────────────────────────────────────

# return ... if request.client else "unknown"
# Имя ведра для запроса вообще без peer-адреса. Значение наружу не выходит и
# ни с чем не сравнивается — важно лишь, что оно одно и то же для всех таких
# запросов, а это верно при любой строке.
f6ba5bb0  # имя ведра-заглушки: наружу не выходит, важна только его одинаковость

# ── db._pick_survivor ───────────────────────────────────────────────────────

# return (1 if row["is_pro"] else 0, ...)  ->  (2 if ... else 0, ...)
# Первый элемент ключа сортировки — признак «есть подписка», сравнивается только
# с нулём. Любое положительное число даёт тот же порядок.
14ef88f4  # вес признака подписки: порядок сортировки от его величины не зависит

# ── config._login_methods_report ────────────────────────────────────────────
#
# Функция возвращает текст предупреждений, который дальше только уходит в лог.
# Все девять мутантов ниже обёртывают формулировку в XX...XX или меняют
# разделитель в перечислении — на смысл это не влияет. Тесты проверяют то, что
# в сообщении действительно важно: имя переменной окружения или провайдера, по
# которому оператор поймёт, что чинить. Пинить дословную формулировку значило бы
# ломать тест при любой правке текста, ничего при этом не стерегя.
#
# Девять записей на одну функцию — это симптом, а не норма: гейт плохо
# приспособлен к коду, который состоит из текста для человека (issue #50).
d981b00b  # формулировка предупреждения про половинчатый Telegram
fe399d1b  # формулировка предупреждения про половинчатый Telegram (вторая строка)
70bcc551  # ярлык провайдера в перечислении настроенных
52e8eaab  # ярлык провайдера в перечислении настроенных
e166ce70  # ярлык провайдера в перечислении настроенных
dde0e47d  # разделитель в перечислении настроенных провайдеров
f39dab42  # формулировка предупреждения про скрытые кнопки
51784c22  # формулировка предупреждения про отсутствующий секрет Яндекса
56ed53a3  # формулировка предупреждения про отсутствующий секрет Mail.ru

# ── db.migrate ──────────────────────────────────────────────────────────────

# applied += 1  ->  applied = 1  (в ветке первого шага)
# Шаги миграций кумулятивны: шаг 1 никогда не выполняется в одиночку — база,
# которой он нужен, нуждается и во всех последующих. Поэтому присваивание в
# его ветке даёт тот же итог, что и накопление: следующий шаг всё равно
# сделает += 1. Запись перестанет быть верной, если появится способ применить
# только шаг 1 — тогда её надо удалить, а не обновлять хеш.
95a62a27  # первый шаг миграции никогда не применяется в одиночку

# ── db._migration_2_drop_telegram_columns ───────────────────────────────────
#
# Проверка «остались ли колонки Telegram» перечисляет все три имени, но в
# реальной схеме они всегда существуют вместе: их создавал один и тот же
# CREATE TABLE. Подмена любого одного имени поэтому не меняет результат
# пересечения — таблица либо старая и содержит все три, либо новая и не
# содержит ни одной. Условие целиком (&, а не |) проверяется тестом
# test_migration_2_guard_short_circuits.
1594002f  # имя колонки в проверке: все три всегда существуют вместе
23b078c2  # имя колонки в проверке: все три всегда существуют вместе
45b116df  # имя колонки в проверке: все три всегда существуют вместе

# ── config._parse_admin_ips ─────────────────────────────────────────────────

# ipaddress.ip_network(item, strict=False)  ->  strict=None
# В ip_network флаг проверяется на истинность (`if strict: raise`), поэтому
# None ведёт себя ровно как False: «203.0.113.7/24» так же не роняет разбор.
933b2406  # strict=None и strict=False для ip_network — одно и то же
# ── main.call_ai ─────────────────────────────────────────────────────────
#
# Сопоставление сбоя с HTTP-кодом клиенту протестировано напрямую (см.
# test_call_ai_*_maps_to_* в tests/test_logic.py, через httpx.MockTransport —
# это реальное наблюдаемое поведение). Ниже — единственный оставшийся
# мутант, который таким тестом не поймать: длина среза тела ответа перед
# логом/проверкой признака OOM. Тот же случай, что и byte-предел в
# _fetch_job_text выше: подстроки "killed"/"terminated" оказываются далеко
# внутри первых 500 символов у любого реального тела ответа, один символ
# границы ничего не решает.
8100bec7  # длина среза тела ошибки перед логом: подстроки OOM всё равно внутри

# ── main._parse_ai ───────────────────────────────────────────────────────
#
# Резервный разбор (find/rfind) протестирован напрямую на реальных сценариях:
# JSON, обёрнутый пояснением, вложенные фигурные скобки, отсутствие скобок
# вовсе, невалидное содержимое между скобками, JSON на позиции 1 и мусор
# сразу после закрывающей скобки (tests/test_logic.py). Ниже — то, что этими
# сценариями объективно не отличить.
#
# removeprefix/removesuffix-цепочка снятия ```json/```-обёртки: что бы ни
# сделала мутация с порядком или целью этих трёх вызовов, при реальном
# ```json ... ``` в ответе итоговый cleaned либо парсится сразу, либо
# первый json.loads падает и в дело вступает резервный find/rfind — а он
# вырезает {...} независимо от того, остался ли обрывок обратных кавычек
# снаружи. Наблюдаемый результат (_parse_ai возвращает тот же dict) от этой
# мутации не меняется — резервный разбор «самолечит» разницу.
73c6d49f  # порядок/цель removeprefix/removesuffix: разницу самолечит резервный find/rfind
59798751  # порядок/цель removeprefix/removesuffix: разницу самолечит резервный find/rfind
0d4ea3e4  # порядок/цель removeprefix/removesuffix: разницу самолечит резервный find/rfind
ba5807e5  # порядок/цель removeprefix/removesuffix: разницу самолечит резервный find/rfind
730637f9  # порядок/цель removeprefix/removesuffix: разницу самолечит резервный find/rfind
d07fcc9c  # порядок/цель removeprefix/removesuffix: разницу самолечит резервный find/rfind

# if start != -1 and end > start:  ->  or / !=-2 / >=
# `end > start` не может стать `end >= start`: start и end — позиции разных
# символов (find("{") и rfind("}")), совпасть не могут, если оба найдены;
# если хоть один не найден — левая часть уже решает результат. `!=-2`
# аналогично: str.find() никогда не возвращает -2, так что "start != -2"
# истинно всегда, кроме случая start == -1 — а туда попасть можно только
# через `or`, и тогда start=-1 используется как отрицательный индекс среза
# (Python "с конца строки"): для любого реалистичного ответа модели такой
# обрывок случайно не складывается в валидный JSON, и итог — тот же
# HTTPException(502), что и без захода в эту ветку вовсе.
f4c8ded8  # and->or: start=-1 как отрицательный индекс среза не даёт валидный JSON случайно
635e3b78  # !=-2 никогда не совпадает с тем, что реально возвращает str.find()
bb2d5c85  # >= вместо >: start и end — позиции разных символов, равными не бывают

# ── prompts._match_prompt / _general_prompt / _generate_prompt ─────────────
#
# Тот же случай, что и config._login_methods_report выше (issue #50): функции
# почти целиком состоят из текста инструкции для модели. Реальную логику —
# какое поле профиля куда попадает, разделитель между несколькими записями
# опыта/образования, дефолты сигнатуры, поведение при отсутствующих полях —
# tests/test_prompts.py проверяет впрямую (по значению в тексте промпта, а
# не по дословной формулировке). Ниже — то, что осталось: сама формулировка
# fallback-текста "не указан(о)"/"определи сам по опыту". Мутант оборачивает
# её в XX...XX, то есть исходная подстрока остаётся внутри — тест по
# вхождению ("не указан" in prompt) её не отличит и отличать не должен:
# модели всё равно, каким именно текстом сформулирован fallback, лишь бы
# понятие «не указано» в нём было. Пиновать точную формулировку — ломать
# тест при любой правке текста, ничего при этом не стерегя.
7b8cfade  # формулировка fallback "не указан" (опыт; profile.get — match/general_prompt)
c4257913  # формулировка fallback "не указано" (образование; profile.get — match/general_prompt)
7da093e2  # формулировка fallback "определи сам по опыту"
0304a1a7  # формулировка fallback "не указан" (опыт; r.experience — generate_prompt)
1eef9c0a  # формулировка fallback "не указано" (образование; r.education — generate_prompt)

# ── main._ensure_resume_capacity ────────────────────────────────────────────

# raise ValueError("resume_limit")  ->  raise ValueError("XXresume_limitXX")
# Все вызывающие (match_to_job, start_match_to_job, generate, generate_from_profile
# и mcp_adapt_resume) разбирают причину подстрокой — `if "resume_limit" in
# str(e):` — а не точным совпадением. "XXresume_limitXX" содержит
# "resume_limit" как подстроку, поэтому проверка проходит так же и
# наблюдаемый ответ (402, {"error": "resume_limit"}) не меняется. Соседний
# мутант этой же строки (замена текста на None) остаётся под гейтом и ловится
# тестом — str(None) уже не содержит "resume_limit", там подстрока рвётся.
ae37e370  # XX-обёртка сообщения: "resume_limit" остаётся подстрокой, проверку `in` не отличить

# ── main._insert_pending_resume ─────────────────────────────────────────────

# company = _resume_group_name(req.company, kind)  ->  ...(req.company, None)
# kind в этом вызове всегда литерал "matched" (присваивается строкой выше).
# Внутри _resume_group_name kind участвует только в сравнении == "general" —
# а "matched" им не является точно так же, как и None. Ветка не меняется ни
# при каком company.
8daac57a  # kind — литерал "matched" в этом вызове; сравнение с "general" даёт тот же исход, что и None

# json.dumps(resume, ensure_ascii=False)  ->  ensure_ascii=None
# Тот же случай, что и config._parse_admin_ips/strict=None выше: json.dumps
# проверяет ensure_ascii на истинность, а не на конкретное значение — False и
# None одинаково ложны, результат для любого содержимого совпадает побайтово
# (проверено: не-ASCII текст не экранируется в обоих случаях).
5dfe8d03  # ensure_ascii=None ведёт себя как False — json.dumps проверяет флаг на истинность

# ── main._mark_generation_failed ────────────────────────────────────────────

# (json.dumps(resume, ensure_ascii=False), resume_id, user_id)  ->  ensure_ascii=None
# Тот же случай ensure_ascii=None/False, что и выше.
d1176395  # ensure_ascii=None ведёт себя как False — json.dumps проверяет флаг на истинность

# ── main._save_resume ───────────────────────────────────────────────────────

# json.dumps(resume, ensure_ascii=False), kind, now)  ->  ensure_ascii=None
# Тот же случай ensure_ascii=None/False, что и выше.
97bccde8  # ensure_ascii=None ведёт себя как False — json.dumps проверяет флаг на истинность

# ── main._robokassa_receipt ─────────────────────────────────────────────────

# ensure_ascii=False -> ensure_ascii=None. json.dumps проверяет значение на
# истинность, поэтому оба варианта дают идентичную UTF-8 JSON-строку.
be528ac7  # ensure_ascii=None и False дают один и тот же JSON

# quote(raw, safe="") -> quote(raw, safe="XXXX"). Буква X входит в набор
# unreserved-символов RFC 3986 и quote не кодирует её независимо от safe;
# добавление X в safe поэтому не меняет результат ни для какой строки.
ac02f3b0  # X и без параметра safe всегда остаётся незакодированной

# ── main._finish_match_generation ───────────────────────────────────────────

# (json.dumps(resume, ensure_ascii=False), resolved_text[:300], resume_id, user_id)  ->  ensure_ascii=None
# Тот же случай ensure_ascii=None/False, что и выше — успешная запись готового
# резюме в фоновой генерации.
4d4069bf  # ensure_ascii=None ведёт себя как False — json.dumps проверяет флаг на истинность
