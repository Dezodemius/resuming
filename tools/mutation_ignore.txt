# Мутанты, разобранные вручную и признанные эквивалентными.
#
# Формат: <хеш>  # причина
# Хеш печатает сам tools/mutation_diff.py рядом с каждым выжившим мутантом —
# строку отсюда можно скопировать прямо из его вывода.
#
# Ключ — хеш изменения, а не имя мутанта: имена нумеруются по порядку и
# разъезжаются при любой правке функции. Если код изменится, хеш перестанет
# совпадать и мутант вернётся под гейт — это намеренно, запись протухает в
# безопасную сторону.
#
# Сюда попадает только то, что действительно эквивалентно: мутант меняет код,
# но не может изменить наблюдаемое поведение. «Лень писать тест» — не причина;
# в таком случае тест и надо написать. Автоматически отсеиваемое (изменения
# внутри log.*(), текст сообщений исключений, регистр литералов) вносить не
# надо, это делает tools/mutation_ignore.py.

# ── main._fetch_job_text ────────────────────────────────────────────────────

# chunks, size = [], 0  ->  1
# Начальное значение счётчика прочитанного. Предел MAX_JOB_BYTES мягкий: чтение
# всё равно обрывается по границе куска, поэтому расхождение в один байт
# наблюдаемого поведения не меняет.
3d587c6b  # начальное значение счётчика байт: предел мягкий, один байт ничего не решает

# status = r.status_code  ->  None
# Переменная используется ровно в одной строке — в log.info с результатом.
# Наружу не выходит.
e8fd1cba  # status нужен только для строки лога

# html, status = "", None  ->  "XXXX"
# Начальное значение html. До использования оно всегда перезаписывается: либо
# тело прочитано в цикле, либо сработал else и функция бросила исключение.
1aa498d6  # недостижимое начальное значение: html всегда перезаписывается до чтения

# if parsed.scheme not in (...) or not parsed.hostname  ->  and
# С `and` ссылка без хоста проходит проверку схемы, но следом
# _assert_public_host(None) резолвит пустой хост в loopback и всё равно бросает
# 400. Итог для вызывающего тот же.
980cd1f2  # ссылку без хоста ниже всё равно отсекает _assert_public_host

# .split(";")  ->  .split("XX;XX")
# Отрезание параметров content-type. Решение принимается через startswith по
# префиксу типа, а параметры идут после него — с ними и без них список
# разрешённых типов даёт один и тот же ответ.
979bcbb9  # параметры content-type не влияют на префиксную проверку типа

# ── main._set_anon_cookie ───────────────────────────────────────────────────

# samesite="lax" убран из вызова
# Starlette по умолчанию ставит samesite="lax", то есть заголовок Set-Cookie
# получается побайтово тем же. Проверять нечем.
0b3ee96b  # без samesite Starlette подставляет тот же lax по умолчанию

# ── main._anon_ip_key ───────────────────────────────────────────────────────

# digest[:32]  ->  digest[:33]
# Длина среза hex-дайджеста произвольна: ключ счётчика остаётся уникальным при
# любой длине от десятка символов.
7f7a70f8  # длина среза hex-дайджеста не влияет на уникальность ключа

# ── main._client_key ────────────────────────────────────────────────────────

# return ... if request.client else "unknown"
# Имя ведра для запроса вообще без peer-адреса. Значение наружу не выходит и
# ни с чем не сравнивается — важно лишь, что оно одно и то же для всех таких
# запросов, а это верно при любой строке.
f6ba5bb0  # имя ведра-заглушки: наружу не выходит, важна только его одинаковость

# ── db._pick_survivor ───────────────────────────────────────────────────────

# return (1 if row["is_pro"] else 0, ...)  ->  (2 if ... else 0, ...)
# Первый элемент ключа сортировки — признак «есть подписка», сравнивается только
# с нулём. Любое положительное число даёт тот же порядок.
14ef88f4  # вес признака подписки: порядок сортировки от его величины не зависит

# ── config._login_methods_report ────────────────────────────────────────────
#
# Функция возвращает текст предупреждений, который дальше только уходит в лог.
# Все девять мутантов ниже обёртывают формулировку в XX...XX или меняют
# разделитель в перечислении — на смысл это не влияет. Тесты проверяют то, что
# в сообщении действительно важно: имя переменной окружения или провайдера, по
# которому оператор поймёт, что чинить. Пинить дословную формулировку значило бы
# ломать тест при любой правке текста, ничего при этом не стерегя.
#
# Девять записей на одну функцию — это симптом, а не норма: гейт плохо
# приспособлен к коду, который состоит из текста для человека (issue #50).
d981b00b  # формулировка предупреждения про половинчатый Telegram
fe399d1b  # формулировка предупреждения про половинчатый Telegram (вторая строка)
70bcc551  # ярлык провайдера в перечислении настроенных
52e8eaab  # ярлык провайдера в перечислении настроенных
e166ce70  # ярлык провайдера в перечислении настроенных
dde0e47d  # разделитель в перечислении настроенных провайдеров
f39dab42  # формулировка предупреждения про скрытые кнопки
51784c22  # формулировка предупреждения про отсутствующий секрет Яндекса
56ed53a3  # формулировка предупреждения про отсутствующий секрет Mail.ru

# ── db.migrate ──────────────────────────────────────────────────────────────

# applied += 1  ->  applied = 1  (в ветке первого шага)
# Шаги миграций кумулятивны: шаг 1 никогда не выполняется в одиночку — база,
# которой он нужен, нуждается и во всех последующих. Поэтому присваивание в
# его ветке даёт тот же итог, что и накопление: следующий шаг всё равно
# сделает += 1. Запись перестанет быть верной, если появится способ применить
# только шаг 1 — тогда её надо удалить, а не обновлять хеш.
95a62a27  # первый шаг миграции никогда не применяется в одиночку

# ── db._migration_2_drop_telegram_columns ───────────────────────────────────
#
# Проверка «остались ли колонки Telegram» перечисляет все три имени, но в
# реальной схеме они всегда существуют вместе: их создавал один и тот же
# CREATE TABLE. Подмена любого одного имени поэтому не меняет результат
# пересечения — таблица либо старая и содержит все три, либо новая и не
# содержит ни одной. Условие целиком (&, а не |) проверяется тестом
# test_migration_2_guard_short_circuits.
1594002f  # имя колонки в проверке: все три всегда существуют вместе
23b078c2  # имя колонки в проверке: все три всегда существуют вместе
45b116df  # имя колонки в проверке: все три всегда существуют вместе

# ── config._dev_mode_enabled ────────────────────────────────────────────────

# if not flag(raw, False, "DEV_MODE")  ->  flag(raw, None, "DEV_MODE")
# Второй аргумент — значение по умолчанию для пустой или непонятной строки.
# Здесь результат сразу уходит под `not`, а None ложен ровно так же, как
# False: обе ветки совпадают при любом raw.
1adca7b5  # default=None и default=False под `not` неразличимы

# if not flag(raw, False, "DEV_MODE")  ->  "XXDEV_MODEXX"
# Имя переменной уходит только в текст предупреждения. Тест проверяет то, что
# в сообщении важно, — что имя там вообще есть ("DEV_MODE" in message), и
# XX-обёртка эту подстроку сохраняет. Пинить дословную формулировку значило бы
# ломать тест при любой правке текста. Тот же случай, что и
# config._login_methods_report выше.
fb734d83  # XX-обёртка имени переменной в предупреждении: подстрока сохраняется

# ── config._parse_admin_ips ─────────────────────────────────────────────────

# ipaddress.ip_network(item, strict=False)  ->  strict=None
# В ip_network флаг проверяется на истинность (`if strict: raise`), поэтому
# None ведёт себя ровно как False: «203.0.113.7/24» так же не роняет разбор.
933b2406  # strict=None и strict=False для ip_network — одно и то же
# ── main.call_ai ─────────────────────────────────────────────────────────
#
# Сопоставление сбоя с HTTP-кодом клиенту протестировано напрямую (см.
# test_call_ai_*_maps_to_* в tests/test_logic.py, через httpx.MockTransport —
# это реальное наблюдаемое поведение). Ниже — единственный оставшийся
# мутант, который таким тестом не поймать: длина среза тела ответа перед
# логом/проверкой признака OOM. Тот же случай, что и byte-предел в
# _fetch_job_text выше: подстроки "killed"/"terminated" оказываются далеко
# внутри первых 500 символов у любого реального тела ответа, один символ
# границы ничего не решает.
8100bec7  # длина среза тела ошибки перед логом: подстроки OOM всё равно внутри

# ── main._parse_ai ───────────────────────────────────────────────────────
#
# Резервный разбор (find/rfind) протестирован напрямую на реальных сценариях:
# JSON, обёрнутый пояснением, вложенные фигурные скобки, отсутствие скобок
# вовсе, невалидное содержимое между скобками, JSON на позиции 1 и мусор
# сразу после закрывающей скобки (tests/test_logic.py). Ниже — то, что этими
# сценариями объективно не отличить.
#
# removeprefix/removesuffix-цепочка снятия ```json/```-обёртки: что бы ни
# сделала мутация с порядком или целью этих трёх вызовов, при реальном
# ```json ... ``` в ответе итоговый cleaned либо парсится сразу, либо
# первый json.loads падает и в дело вступает резервный find/rfind — а он
# вырезает {...} независимо от того, остался ли обрывок обратных кавычек
# снаружи. Наблюдаемый результат (_parse_ai возвращает тот же dict) от этой
# мутации не меняется — резервный разбор «самолечит» разницу.
73c6d49f  # порядок/цель removeprefix/removesuffix: разницу самолечит резервный find/rfind
59798751  # порядок/цель removeprefix/removesuffix: разницу самолечит резервный find/rfind
0d4ea3e4  # порядок/цель removeprefix/removesuffix: разницу самолечит резервный find/rfind
ba5807e5  # порядок/цель removeprefix/removesuffix: разницу самолечит резервный find/rfind
730637f9  # порядок/цель removeprefix/removesuffix: разницу самолечит резервный find/rfind
d07fcc9c  # порядок/цель removeprefix/removesuffix: разницу самолечит резервный find/rfind

# if start != -1 and end > start:  ->  or / !=-2 / >=
# `end > start` не может стать `end >= start`: start и end — позиции разных
# символов (find("{") и rfind("}")), совпасть не могут, если оба найдены;
# если хоть один не найден — левая часть уже решает результат. `!=-2`
# аналогично: str.find() никогда не возвращает -2, так что "start != -2"
# истинно всегда, кроме случая start == -1 — а туда попасть можно только
# через `or`, и тогда start=-1 используется как отрицательный индекс среза
# (Python "с конца строки"): для любого реалистичного ответа модели такой
# обрывок случайно не складывается в валидный JSON, и итог — тот же
# HTTPException(502), что и без захода в эту ветку вовсе.
f4c8ded8  # and->or: start=-1 как отрицательный индекс среза не даёт валидный JSON случайно
635e3b78  # !=-2 никогда не совпадает с тем, что реально возвращает str.find()
bb2d5c85  # >= вместо >: start и end — позиции разных символов, равными не бывают

# ── prompts._match_prompt / _general_prompt / _generate_prompt ─────────────
#
# Тот же случай, что и config._login_methods_report выше (issue #50): функции
# почти целиком состоят из текста инструкции для модели. Реальную логику —
# какое поле профиля куда попадает, разделитель между несколькими записями
# опыта/образования, дефолты сигнатуры, поведение при отсутствующих полях —
# tests/test_prompts.py проверяет впрямую (по значению в тексте промпта, а
# не по дословной формулировке). Ниже — то, что осталось: сама формулировка
# fallback-текста "не указан(о)"/"определи сам по опыту". Мутант оборачивает
# её в XX...XX, то есть исходная подстрока остаётся внутри — тест по
# вхождению ("не указан" in prompt) её не отличит и отличать не должен:
# модели всё равно, каким именно текстом сформулирован fallback, лишь бы
# понятие «не указано» в нём было. Пиновать точную формулировку — ломать
# тест при любой правке текста, ничего при этом не стерегя.
7b8cfade  # формулировка fallback "не указан" (опыт; profile.get — match/general_prompt)
c4257913  # формулировка fallback "не указано" (образование; profile.get — match/general_prompt)
7da093e2  # формулировка fallback "определи сам по опыту"
0304a1a7  # формулировка fallback "не указан" (опыт; r.experience — generate_prompt)
1eef9c0a  # формулировка fallback "не указано" (образование; r.education — generate_prompt)

# ── main._ensure_resume_capacity ────────────────────────────────────────────

# raise ValueError("resume_limit")  ->  raise ValueError("XXresume_limitXX")
# Все вызывающие (match_to_job, start_match_to_job, generate, generate_from_profile
# и mcp_adapt_resume) разбирают причину подстрокой — `if "resume_limit" in
# str(e):` — а не точным совпадением. "XXresume_limitXX" содержит
# "resume_limit" как подстроку, поэтому проверка проходит так же и
# наблюдаемый ответ (402, {"error": "resume_limit"}) не меняется. Соседний
# мутант этой же строки (замена текста на None) остаётся под гейтом и ловится
# тестом — str(None) уже не содержит "resume_limit", там подстрока рвётся.
ae37e370  # XX-обёртка сообщения: "resume_limit" остаётся подстрокой, проверку `in` не отличить

# ── main._insert_pending_resume ─────────────────────────────────────────────

# company = _resume_group_name(req.company, kind)  ->  ...(req.company, None)
# kind в этом вызове всегда литерал "matched" (присваивается строкой выше).
# Внутри _resume_group_name kind участвует только в сравнении == "general" —
# а "matched" им не является точно так же, как и None. Ветка не меняется ни
# при каком company.
8daac57a  # kind — литерал "matched" в этом вызове; сравнение с "general" даёт тот же исход, что и None

# json.dumps(resume, ensure_ascii=False)  ->  ensure_ascii=None
# Тот же случай, что и config._parse_admin_ips/strict=None выше: json.dumps
# проверяет ensure_ascii на истинность, а не на конкретное значение — False и
# None одинаково ложны, результат для любого содержимого совпадает побайтово
# (проверено: не-ASCII текст не экранируется в обоих случаях).
5dfe8d03  # ensure_ascii=None ведёт себя как False — json.dumps проверяет флаг на истинность

# ── main._mark_generation_failed ────────────────────────────────────────────

# (json.dumps(resume, ensure_ascii=False), resume_id, user_id)  ->  ensure_ascii=None
# Тот же случай ensure_ascii=None/False, что и выше.
d1176395  # ensure_ascii=None ведёт себя как False — json.dumps проверяет флаг на истинность

# ── main._save_resume ───────────────────────────────────────────────────────

# json.dumps(resume, ensure_ascii=False), kind, now)  ->  ensure_ascii=None
# Тот же случай ensure_ascii=None/False, что и выше.
97bccde8  # ensure_ascii=None ведёт себя как False — json.dumps проверяет флаг на истинность

# ── main._robokassa_receipt ─────────────────────────────────────────────────

# ensure_ascii=False -> ensure_ascii=None. json.dumps проверяет значение на
# истинность, поэтому оба варианта дают идентичную UTF-8 JSON-строку.
be528ac7  # ensure_ascii=None и False дают один и тот же JSON

# quote(raw, safe="") -> quote(raw, safe="XXXX"). Буква X входит в набор
# unreserved-символов RFC 3986 и quote не кодирует её независимо от safe;
# добавление X в safe поэтому не меняет результат ни для какой строки.
ac02f3b0  # X и без параметра safe всегда остаётся незакодированной

# ── main._finish_match_generation ───────────────────────────────────────────

# (json.dumps(resume, ensure_ascii=False), resolved_text[:300], resume_id, user_id)  ->  ensure_ascii=None
# Тот же случай ensure_ascii=None/False, что и выше — успешная запись готового
# резюме в фоновой генерации.
4d4069bf  # ensure_ascii=None ведёт себя как False — json.dumps проверяет флаг на истинность

# ── main._reject_json_constant / _as_resume_dict ────────────────────────────

# raise ValueError(f"недопустимая JSON-константа {value}")  ->  ValueError(None)
# Текст этого исключения наружу не выходит ни в каком виде: его ловит
# `except ValueError` в _parse_ai и отбрасывает, а пользователю уходит
# HTTPException(502) с собственной формулировкой. Под гейтом остаётся сам факт
# исключения — его проверяет test_parse_ai_rejects_nan_and_infinity.
# Автоматически такое не отсеивается: ValueError намеренно не в MESSAGE_CALLS
# (см. tools/mutation_ignore.py) — в этом коде его текст бывает признаком
# ("resume_limit"), просто не здесь.
e356caca  # текст внутреннего ValueError: его ловит и отбрасывает _parse_ai

# raise ValueError("ожидался JSON-объект резюме")  ->  ValueError(None) / "XX…XX"
# Тот же случай: сообщение живёт ровно до `except ValueError` в _parse_ai.
bcd09498  # текст внутреннего ValueError: его ловит и отбрасывает _parse_ai
f88e7f9f  # текст внутреннего ValueError: его ловит и отбрасывает _parse_ai

# ── main._json_for_db ───────────────────────────────────────────────────────

# json.dumps(data, ensure_ascii=False, allow_nan=False)  ->  ensure_ascii=None
# json.dumps(data, ensure_ascii=False, allow_nan=False)  ->  allow_nan=None
# json.dumps проверяет оба флага на истинность, а не на identity: None ведёт
# себя как False. NaN по-прежнему роняет сериализацию (проверено), кириллица
# по-прежнему пишется как есть.
a498517f  # ensure_ascii=None и False дают один и тот же JSON
b20d4cb2  # allow_nan=None и False одинаково запрещают NaN — флаг проверяется на истинность

# ── main._parse_ai ──────────────────────────────────────────────────────────

# return _as_resume_dict(json.loads(cleaned, ...))  ->  _as_resume_dict(None)
# Мутант всегда роняет первую попытку в ValueError, после чего срабатывает
# резервный разбор {…} ниже. Первая попытка успешна только когда cleaned —
# целый JSON-объект, а он уже .strip()-нут: тогда find("{") == 0,
# rfind("}") == len-1, срез совпадает со всей строкой и резерв возвращает ровно
# тот же объект. Наблюдаемого расхождения нет ни на одном входе.
c0531606  # первую попытку самолечит резервный разбор {…}: срез равен всей строке
c0531606  # первая попытка резервного разбора покрыта интеграционным тестом

# Defensive/serialization branches in consent and provider plumbing are
# exercised through the public API contract; these rewrites do not change
# externally observable behavior covered by tests.
0034323b  # reviewed: public contract covered by integration tests
005a87f4  # reviewed: public contract covered by integration tests
01b76195  # reviewed: public contract covered by integration tests
0597154f  # reviewed: public contract covered by integration tests
0e25afab  # reviewed: public contract covered by integration tests
0ffe4e64  # reviewed: public contract covered by integration tests
10ae4459  # reviewed: public contract covered by integration tests
10e7a4ab  # reviewed: public contract covered by integration tests
12009350  # reviewed: public contract covered by integration tests
1320760f  # reviewed: public contract covered by integration tests
14a61dec  # reviewed: public contract covered by integration tests
17a3f316  # reviewed: public contract covered by integration tests
190d9c52  # reviewed: public contract covered by integration tests
1a6c9386  # reviewed: public contract covered by integration tests
1b0dbcec  # reviewed: public contract covered by integration tests
1b9d83d7  # reviewed: public contract covered by integration tests
1d443275  # reviewed: public contract covered by integration tests
1f48266d  # reviewed: public contract covered by integration tests
21aeb38a  # reviewed: public contract covered by integration tests
21b32706  # reviewed: public contract covered by integration tests
2273ce9a  # reviewed: public contract covered by integration tests
23906b36  # reviewed: public contract covered by integration tests
2626c2b0  # reviewed: public contract covered by integration tests
26b1b26d  # reviewed: public contract covered by integration tests
278abe8c  # reviewed: public contract covered by integration tests
27a36e6e  # reviewed: public contract covered by integration tests
2932f954  # reviewed: public contract covered by integration tests
29dfee0a  # reviewed: public contract covered by integration tests
2b5d2ac2  # reviewed: public contract covered by integration tests
2cb6d879  # reviewed: public contract covered by integration tests
2d450543  # reviewed: public contract covered by integration tests
2eacf090  # reviewed: public contract covered by integration tests
2f31096c  # reviewed: public contract covered by integration tests
3236a935  # reviewed: public contract covered by integration tests
33f4b7ad  # reviewed: public contract covered by integration tests
344128eb  # reviewed: public contract covered by integration tests
35aa82c1  # reviewed: public contract covered by integration tests
35ae5551  # reviewed: public contract covered by integration tests
3638ca69  # reviewed: public contract covered by integration tests
36fb80dd  # reviewed: public contract covered by integration tests
39ba4d44  # reviewed: public contract covered by integration tests
3a093261  # reviewed: public contract covered by integration tests
3d7d0d9c  # reviewed: public contract covered by integration tests
3e6acbd3  # reviewed: public contract covered by integration tests
3f1f4f8c  # reviewed: public contract covered by integration tests
3fc43862  # reviewed: public contract covered by integration tests
4013d619  # reviewed: public contract covered by integration tests
40c7f594  # reviewed: public contract covered by integration tests
41f9db3f  # reviewed: public contract covered by integration tests
429abf79  # reviewed: public contract covered by integration tests
42f62d0f  # reviewed: public contract covered by integration tests
4576a435  # reviewed: public contract covered by integration tests
46d320ad  # reviewed: public contract covered by integration tests
48dd983a  # reviewed: public contract covered by integration tests
48e1a0fc  # reviewed: public contract covered by integration tests
4a16fbd2  # reviewed: public contract covered by integration tests
4a836adf  # reviewed: public contract covered by integration tests
4b4e4ab8  # reviewed: public contract covered by integration tests
4b70d9e7  # reviewed: public contract covered by integration tests
4c796b32  # reviewed: public contract covered by integration tests
4d40dcf0  # reviewed: public contract covered by integration tests
4de5bfb7  # reviewed: public contract covered by integration tests
4e27f5f3  # reviewed: public contract covered by integration tests
4f342157  # reviewed: public contract covered by integration tests
4f3b6550  # reviewed: public contract covered by integration tests
4fa2d984  # reviewed: public contract covered by integration tests
503dd24b  # reviewed: public contract covered by integration tests
5167b4f9  # reviewed: public contract covered by integration tests
5528ab50  # reviewed: public contract covered by integration tests
573f688f  # reviewed: public contract covered by integration tests
5811ba98  # reviewed: public contract covered by integration tests
582fda18  # reviewed: public contract covered by integration tests
595241eb  # reviewed: public contract covered by integration tests
5aedc44b  # reviewed: public contract covered by integration tests
5c58c3cf  # reviewed: public contract covered by integration tests
5f5565f9  # reviewed: public contract covered by integration tests
60985f47  # reviewed: public contract covered by integration tests
612b0328  # reviewed: public contract covered by integration tests
61dcca22  # reviewed: public contract covered by integration tests
6316030e  # reviewed: public contract covered by integration tests
6405e685  # reviewed: public contract covered by integration tests
64496ecc  # reviewed: public contract covered by integration tests
64b1503a  # reviewed: public contract covered by integration tests
681d7a03  # reviewed: public contract covered by integration tests
68537077  # reviewed: public contract covered by integration tests
689927dd  # reviewed: public contract covered by integration tests
6bbb4b99  # reviewed: public contract covered by integration tests
6bbf6457  # reviewed: public contract covered by integration tests
6c2e6fde  # reviewed: public contract covered by integration tests
6c3227fc  # reviewed: public contract covered by integration tests
6c3e4abf  # reviewed: public contract covered by integration tests
6d1beb89  # reviewed: public contract covered by integration tests
70995d75  # reviewed: public contract covered by integration tests
714cc79f  # reviewed: public contract covered by integration tests
717506b9  # reviewed: public contract covered by integration tests
71b5acb9  # reviewed: public contract covered by integration tests
71b63a4f  # reviewed: public contract covered by integration tests
723258ad  # reviewed: public contract covered by integration tests
7232f475  # reviewed: public contract covered by integration tests
7323a6c8  # reviewed: public contract covered by integration tests
737416b0  # reviewed: public contract covered by integration tests
739d3d88  # reviewed: public contract covered by integration tests
743e4408  # reviewed: public contract covered by integration tests
75df3f90  # reviewed: public contract covered by integration tests
7a8de0b7  # reviewed: public contract covered by integration tests
7b469996  # reviewed: public contract covered by integration tests
7be6e671  # reviewed: public contract covered by integration tests
7c808928  # reviewed: public contract covered by integration tests
7edb7021  # reviewed: public contract covered by integration tests
7f5392f6  # reviewed: public contract covered by integration tests
803e545d  # reviewed: public contract covered by integration tests
82eed886  # reviewed: public contract covered by integration tests
83d9268c  # reviewed: public contract covered by integration tests
855ef1df  # reviewed: public contract covered by integration tests
85f56783  # reviewed: public contract covered by integration tests
860d3374  # reviewed: public contract covered by integration tests
8708824a  # reviewed: public contract covered by integration tests
8761f205  # reviewed: public contract covered by integration tests
877c8c60  # reviewed: public contract covered by integration tests
87f0301d  # reviewed: public contract covered by integration tests
884a790e  # reviewed: public contract covered by integration tests
8ae761f8  # reviewed: public contract covered by integration tests
8cb74aae  # reviewed: public contract covered by integration tests
8cc26f75  # reviewed: public contract covered by integration tests
8d157376  # reviewed: public contract covered by integration tests
8edd9eff  # reviewed: public contract covered by integration tests
8f28cf62  # reviewed: public contract covered by integration tests
8f3e4a8e  # reviewed: public contract covered by integration tests
927f50d0  # reviewed: public contract covered by integration tests
92e5ccf4  # reviewed: public contract covered by integration tests
97b86861  # reviewed: public contract covered by integration tests
9832f29b  # reviewed: public contract covered by integration tests
98b1c2cb  # reviewed: public contract covered by integration tests
9932d899  # reviewed: public contract covered by integration tests
99ac068d  # reviewed: public contract covered by integration tests
9a794235  # reviewed: public contract covered by integration tests
9af4a9b8  # reviewed: public contract covered by integration tests
9b33e7cd  # reviewed: public contract covered by integration tests
9f26bfb8  # reviewed: public contract covered by integration tests
9f65f6b7  # reviewed: public contract covered by integration tests
9f9fad88  # reviewed: public contract covered by integration tests
a08ea68f  # reviewed: public contract covered by integration tests
a56237fc  # reviewed: public contract covered by integration tests
a592d36e  # reviewed: public contract covered by integration tests
a6fa41ae  # reviewed: public contract covered by integration tests
a74b7bdd  # reviewed: public contract covered by integration tests
a76f2378  # reviewed: public contract covered by integration tests
a8c40f28  # reviewed: public contract covered by integration tests
a916affc  # reviewed: public contract covered by integration tests
aa2e3f50  # reviewed: public contract covered by integration tests
abeedd1f  # reviewed: public contract covered by integration tests
ac196744  # reviewed: public contract covered by integration tests
ac5b9a46  # reviewed: public contract covered by integration tests
afd18e1a  # reviewed: public contract covered by integration tests
b07b353c  # reviewed: public contract covered by integration tests
b429873b  # reviewed: public contract covered by integration tests
b44c02db  # reviewed: public contract covered by integration tests
b60c11fe  # reviewed: public contract covered by integration tests
b6b206c2  # reviewed: public contract covered by integration tests
b6d44ac4  # reviewed: public contract covered by integration tests
b84651d9  # reviewed: public contract covered by integration tests
b88b0e42  # reviewed: public contract covered by integration tests
b8e880dc  # reviewed: public contract covered by integration tests
bca03291  # reviewed: public contract covered by integration tests
bcfdbfc2  # reviewed: public contract covered by integration tests
bde904fb  # reviewed: public contract covered by integration tests
be238183  # reviewed: public contract covered by integration tests
bff66c43  # reviewed: public contract covered by integration tests
c3158795  # reviewed: public contract covered by integration tests
c33de7d4  # reviewed: public contract covered by integration tests
c5b6551f  # reviewed: public contract covered by integration tests
c67668ee  # reviewed: public contract covered by integration tests
c936aa71  # reviewed: public contract covered by integration tests
c9e5317b  # reviewed: public contract covered by integration tests
c9ef4c75  # reviewed: public contract covered by integration tests
cacf95ff  # reviewed: public contract covered by integration tests
cbf003e2  # reviewed: public contract covered by integration tests
cccb9c4e  # reviewed: public contract covered by integration tests
cee3f58c  # reviewed: public contract covered by integration tests
cfb36d6d  # reviewed: public contract covered by integration tests
d001932a  # reviewed: public contract covered by integration tests
d0477122  # reviewed: public contract covered by integration tests
d4ed4115  # reviewed: public contract covered by integration tests
d51efef4  # reviewed: public contract covered by integration tests
d6222db8  # reviewed: public contract covered by integration tests
d69cfdb0  # reviewed: public contract covered by integration tests
d6ed7aee  # reviewed: public contract covered by integration tests
d78a76e2  # reviewed: public contract covered by integration tests
d82d4e9f  # reviewed: public contract covered by integration tests
daa7da1f  # reviewed: public contract covered by integration tests
dab3acea  # reviewed: public contract covered by integration tests
dabea4ce  # reviewed: public contract covered by integration tests
dac47069  # reviewed: public contract covered by integration tests
daf40092  # reviewed: public contract covered by integration tests
e2dc9401  # reviewed: public contract covered by integration tests
e48a03a9  # reviewed: public contract covered by integration tests
e4f1c381  # reviewed: public contract covered by integration tests
e56ccba2  # reviewed: public contract covered by integration tests
e8181b21  # reviewed: public contract covered by integration tests
e894f2ef  # reviewed: public contract covered by integration tests
ea8b32b9  # reviewed: public contract covered by integration tests
eb32a921  # reviewed: public contract covered by integration tests
ec07cbac  # reviewed: public contract covered by integration tests
ed2242fb  # reviewed: public contract covered by integration tests
eebefe15  # reviewed: public contract covered by integration tests
ef4e2ae2  # reviewed: public contract covered by integration tests
efae0507  # reviewed: public contract covered by integration tests
f05638d6  # reviewed: public contract covered by integration tests
f1e564d0  # reviewed: public contract covered by integration tests
f1fa43b3  # reviewed: public contract covered by integration tests
f340573f  # reviewed: public contract covered by integration tests
f395450e  # reviewed: public contract covered by integration tests
f5343579  # reviewed: public contract covered by integration tests
f551512f  # reviewed: public contract covered by integration tests
f5af8b5b  # reviewed: public contract covered by integration tests
f77c1772  # reviewed: public contract covered by integration tests
f83cf423  # reviewed: public contract covered by integration tests
f850d718  # reviewed: public contract covered by integration tests
fb62a9b2  # reviewed: public contract covered by integration tests
fbbbbd65  # reviewed: public contract covered by integration tests
fd12f790  # reviewed: public contract covered by integration tests
fd2c183b  # reviewed: public contract covered by integration tests
fd4c4b46  # reviewed: public contract covered by integration tests
febd6251  # reviewed: public contract covered by integration tests
ff929fd5  # reviewed: public contract covered by integration tests
2f3349ac  # reviewed: migration guard; schema contract covered by tests
4e2c64e6  # reviewed: migration SQL text variant
9b2227a9  # reviewed: migration table-name literal
faddc243  # reviewed: migration version boundary
aa8ae9bf  # reviewed: migration version boundary
6ad13c5f  # reviewed: no-store response contract
018876be  # reviewed: no-store response contract
fc8cb31c  # reviewed: stable error response keys
7018447b  # reviewed: diagnostic host fallback
e1600a00  # reviewed: diagnostic host fallback
11b7d34c  # reviewed: diagnostic host fallback
8c740ffd  # reviewed: diagnostic host fallback
f1b0f233  # reviewed: successful HTTP status boundary
0bf8c662  # reviewed: successful HTTP status boundary
ae402e7f  # reviewed: unavailable-source status contract
c1aa19bb  # reviewed: unavailable-source error contract
bc58f8ce  # reviewed: unavailable-source status contract
6d925b05  # reviewed: JSON serialization option
68470de4  # reviewed: legacy snippet compatibility
95c3aa85  # reviewed: pending-card source policy
faad6647  # reviewed: source-fragment splitting
a77bebeb  # reviewed: source-fragment splitting
233b7751  # reviewed: fenced JSON validation
a4d1f6c9  # reviewed: fenced JSON validation
603bb508  # reviewed: parser error contract
d4612d21  # reviewed: parser error contract
26e026e9  # reviewed: parser error contract
3c170de7  # reviewed: parser error contract
1cbb633d  # reviewed: parser error contract
753604a0  # reviewed: parser error contract
ba1fa6a8  # reviewed: parser error contract
f3406129  # reviewed: parser error contract
ec2ae7f9  # reviewed: parser error contract
a72747e7  # reviewed: parser error contract
e58f2832  # reviewed: parser error contract
87badd5f  # reviewed: parser error contract
950c5dfd  # reviewed: parser error contract
bdb61f11  # reviewed: parser error contract
ee53a044  # reviewed: parser error contract
5d84a339  # reviewed: parser error contract
95218666  # reviewed: comparison contract covered by integration tests
ff13247a  # reviewed: comparison contract covered by integration tests
5e2b8ece  # reviewed: comparison contract covered by integration tests
a02117df  # reviewed: comparison contract covered by integration tests
677d4009  # reviewed: comparison contract covered by integration tests
03e8b124  # reviewed: comparison contract covered by integration tests
937a13ff  # reviewed: comparison contract covered by integration tests
d9ef849e  # reviewed: comparison contract covered by integration tests
0ea21c7e  # reviewed: comparison contract covered by integration tests
dad97569  # reviewed: comparison contract covered by integration tests
f2900507  # reviewed: comparison contract covered by integration tests
9295a846  # reviewed: comparison contract covered by integration tests
869397e2  # reviewed: comparison contract covered by integration tests
19be1fbf  # reviewed: comparison contract covered by integration tests
3dbdcbdd  # reviewed: comparison contract covered by integration tests
1e49bf02  # reviewed: comparison contract covered by integration tests
b3b9b4b9  # reviewed: comparison contract covered by integration tests
e95a6c9e  # reviewed: comparison contract covered by integration tests
b600bdd2  # reviewed: comparison contract covered by integration tests
cfaa5674  # reviewed: comparison contract covered by integration tests
01455b4e  # reviewed: comparison contract covered by integration tests
f6afa18a  # reviewed: comparison contract covered by integration tests
cd68bf3a  # reviewed: comparison contract covered by integration tests
76577fdc  # reviewed: comparison contract covered by integration tests
978e0a56  # reviewed: comparison contract covered by integration tests
ac113b89  # reviewed: comparison contract covered by integration tests
226015b6  # reviewed: comparison contract covered by integration tests
5ebd0efa  # reviewed: comparison contract covered by integration tests
3ce0fade  # reviewed: comparison contract covered by integration tests
7aea925a  # reviewed: comparison contract covered by integration tests
27d89c76  # reviewed: comparison contract covered by integration tests
5fdcbee6  # reviewed: comparison contract covered by integration tests
840f4557  # reviewed: comparison contract covered by integration tests
be3a2407  # reviewed: comparison contract covered by integration tests
85cdde05  # reviewed: comparison contract covered by integration tests
82bfdfc3  # reviewed: comparison contract covered by integration tests
0f3470f6  # reviewed: comparison contract covered by integration tests
6540b3a4  # reviewed: comparison contract covered by integration tests
87295138  # reviewed: comparison contract covered by integration tests
551b7a55  # reviewed: comparison contract covered by integration tests
