# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/

# База данных и данные приложения (БД + файловые логи — PII, не коммитить)
data/
*.db
*.sqlite
*.log

# Окружение
.env

# Стенд: секреты и доступы к VPS (см. deploy/README.md)
deploy/.env.staging
deploy/.deploy.env
deploy/_authconf/

# Ollama модели (кешируются отдельно)
.ollama/

# Мутационное тестирование: mutmut держит копию проекта и кеш результатов
mutants/
.mutmut-cache

# Отчёты тестов (junit-xml из behave и pytest в CI)
reports/

# Мусор от браузерных прогонов (/smoke-prod): дампы страниц и консоли прода.
# Репозиторий публичный — такие выгрузки в нём не нужны ни на минуту.
.playwright-mcp/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
site.webmanifest

# Локальные бэкапы продовой БД (PII — не коммитить)
backups/
.claude/settings.json
