pytest==8.3.5
pytest-asyncio==0.24.0
anyio==4.6.0
ruff==0.8.0
# BDD-сценарии (tests/bdd, запуск: behave)
behave==1.3.3
# Мутационное тестирование (setup.cfg + tools/mutation_diff.py).
# mutmut 3.x форкается через os.fork() — работает только на Linux/macOS/WSL,
# на Windows команда сообщит об этом и выйдет. Установке это не мешает.
mutmut==3.7.0
