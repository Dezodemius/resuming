[behave]
# Сценарии лежат в tests/bdd/features, шаги — в tests/bdd/steps: behave поднимается
# от каталога с фичами вверх, пока не найдёт steps/ рядом с environment.py.
paths = tests/bdd/features
lang = ru
show_timings = true
show_skipped = false
